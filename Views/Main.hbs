<!DOCTYPE html>
<html lang="en">

    {{> Layout.Head}}
<body>
{{#if useLayout}}

    <div class="header">
        {{> Layout.Navbar}}

        <main>
            {{{ body}}}
        </main>
        {{> Layout.Footer}}
    </div>
    {{> Layout.Js}}

{{else}}
    {{{body}}}
{{/if}}

</body>
</html>
