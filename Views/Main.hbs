<!DOCTYPE html>
<html lang="en">

<body>
{{#if useLayout}}
    {{> Layout.Head}}

    <div class="header">
        {{> Layout.Navbar}}

        <main>
            {{{ body}}}
        </main>
        {{> Layout.Footer}}
    </div>
    {{> Layout.Js}}

{{else}}
    {{{body}}}
{{/if}}

</body>
</html>
